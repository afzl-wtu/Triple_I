import 'package:flutter/material.dart';

class NNotification extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Notefication'),
      ),
    );
  }
}
